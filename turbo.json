{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": [
    "API_SERVER_URL",
    "INTANIA_AUTH_URL",
    "INTANIA_AUTH_APP_ID",
    "INTANIA_AUTH_SECRET"
  ],
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "lint": {},
    "type-check": {},
    "dev": {
      "cache": false,
      "persistent": true
    },
    "clean": {
      "cache": false
    }
  }
}
